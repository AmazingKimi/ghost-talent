# Optional developer fallback. Raises GitHub API rate limits for local scouting.
GITHUB_TOKEN=

# Recommended public-product path. Create a GitHub OAuth App, enable Device Flow,
# then place its public Client ID here. No client secret is required for Device Flow.
GHOST_TALENT_GITHUB_CLIENT_ID=
